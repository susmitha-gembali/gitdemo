name: First Workflow
on: 
  push:
    branches: main
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: echo "Hello,Good Evening"
      - name: Print bye
        run: echo "Bye!"
  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps:
      - name: Welcome msg
        run: echo "This is second job!!!"

    

